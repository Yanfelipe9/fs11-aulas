// Conectivos lógicos
        // && - AND
        // || - OR
        // ! - NOT

        // AND
        // true && true = true
        // true && false = false
        // false && true = false
        // false && false = false

        // OR
        // true || true = true
        // true || false = true
        // false || true = true
        // false || false = false

        // NOT
        // !true = false
        // !false = true

        // Tabela verdade do AND
        // true && true = true
        // true && false = false
        // false && true = false
        // false && false = false

        // Tabela verdade do OR
        // true || true = true
        // true || false = true
        // false || true = true
        // false || false = false

        // Tabela verdade do NOT
        // !true = false
        // !false = true

        // Tabela verdade do XOR
        // true ^ true = false
        // true ^ false = true
        // false ^ true = true
        // false ^ false = false

        // Tabela verdade do NAND
        // !(true && true) = false
        // !(true && false) = true
        // !(false && true) = true
        // !(false && false) = true

        // Tabela verdade do NOR
        // !(true || true) = false
        // !(true || false) = false
        // !(false || true) = false
        // !(false || false) = true

        // Tabela verdade do XNOR
        // !(true ^ true) = true
        // !(true ^ false) = false
        // !(false ^ true) = false
        // !(false ^ false) = true

        // Tabela verdade do IMPLICA
        // true -> true = true
        // true -> false = false
        // false -> true = true
        // false -> false = true

        // Tabela verdade do BICONDICIONAL
        // true <-> true = true
        // true <-> false = false
        // false <-> true = false
        // false <-> false = true